<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="${log.scan}" scanPeriod="60 seconds">
	<contextName>zp_admin</contextName>
	<property name="logDir" value="${log.basedir}" />
	<appender name="console" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%date{HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
		</encoder>
	</appender>
	<appender name="rollingFile"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${logDir}/log.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${logDir}/log%d{yyyy-MM-dd}.log</fileNamePattern>
			<maxHistory>30</maxHistory>
			<totalSizeCap>3GB</totalSizeCap>
		</rollingPolicy>
		<encoder>
			<pattern>%date %-5level %logger{36} - %msg%n</pattern>
			<charset>UTF-8</charset>
		</encoder>
	</appender>

	<logger name="org.springframework" level="warn" />
	<logger name="org.springframework.web" level="warn" />
	<logger name="org.apache.http" level="warn" />
	<logger name="org.apache.ibatis" level="${log.level}" />
	<logger name="org.mybatis.spring" level="warn" />
    <logger name="com.baomidou.mybatisplus" level="${log.level}" />
	<logger name="com.alibaba.dubbo" level="info" />
	<logger name="org.apache.zookeeper" level="info" />

	<root level="${log.level}">
		<appender-ref ref="console" />
		<appender-ref ref="rollingFile" />
	</root>
</configuration>